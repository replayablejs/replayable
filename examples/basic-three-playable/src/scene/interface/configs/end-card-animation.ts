/** Seconds and scale factors shared by the end-card entrance and CTA attention. */
export const endCardAnimation = {
  backdropDuration: 0.14,
  logoDuration: 0.275,
  buttonDuration: 0.16,
  buttonStartScale: 0.86,
  buttonStartOffset: 14,
  breathingDelay: 0.15,
  breathingDuration: 1.8,
  breathingScale: 1.045,
  breathingRepeatDelay: 0.15,
} as const;
