/** Picking surface and hit area, in the city models' world units. */
export const plotInputConfig = {
  // Match the elevation of the placement markers.
  height: 0.065,
  // Each pavement cell is one unit wide and deep.
  halfSize: 0.5,
} as const;
