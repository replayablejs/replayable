# Development sound-control artwork

`sound-on.png` and `sound-off.png` are the shared development sound-control icons. They originated
in the examples as `sound_on.png` and `sound_off.png`. The maintainer confirmed redistribution
permission during alpha preparation. This statement does not assign a new license to the artwork.
The development control owns these copies; production builds select its disabled implementation.
