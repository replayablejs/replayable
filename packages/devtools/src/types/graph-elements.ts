/** Reusable SVG nodes; path calculations remain in the graph. */
export interface StatsGraphElements {
  readonly root: SVGSVGElement;
  readonly fill: SVGPathElement;
  readonly stroke: SVGPathElement;
}
