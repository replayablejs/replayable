# Bundled GPU benchmark data

The benchmark data is copied from `@pmndrs/detect-gpu` version 6.0.20, from its `dist/benchmarks`
directory. It supports mobile GPU classification in exported playables without a network fetch.

The upstream project is copyright Tim van Scherpenzeel and distributed under the MIT License.
Keep [LICENSE](./LICENSE) with these datasets. Repository formatting may differ from upstream;
that does not change their provenance or license.
